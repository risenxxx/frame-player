  <section class="feat sc-cast" id="tv">
    <div class="wrap feat-in">
      <div class="feat-copy" data-reveal>
        <h2>Send it to the set, without repacking it first</h2>
        <p class="lede">
          One button in the control bar, one row per television, and the row says what will happen
          to <i>this</i> file on <i>that</i> device. Where the set takes the release as it is, which
          for a DLNA renderer usually means a 4K HEVC HDR MKV with Dolby audio, that is what it gets.
        </p>
        <ul class="checks">
          <li><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-check"/></svg>
            <span><b>Where it cannot, the file is remuxed first</b> with the video stream-copied, so a film is ready in seconds rather than in an hour.</span></li>
          <li><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-check"/></svg>
            <span><b>A torrent that is still downloading can be cast too</b>, fed from the same piece-priority stream that feeds local playback.</span></li>
          <li><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-check"/></svg>
            <span><b>While it plays, the window is a remote.</b> Keys, chapters and the queue act on the set; disconnecting hands playback back at the position it had.</span></li>
        </ul>
      </div>
      <div class="feat-mock" data-reveal style="--dl:1">
        <div class="win" data-scene data-dur="3600" aria-hidden="true">
          <div class="win-bar"><span class="dots"><i></i><i></i><i></i></span><span class="name mono">Orbital.Night.S02E03.2160p.HDR.mkv</span><span class="wb-brand"><span>Frame Player</span><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#i-mark"/></svg></span></div>
          <div class="player mini">
            <img class="scene" src="/img/moon-eclipse.jpg" srcset="/img/moon-eclipse.jpg 1x, /img/moon-eclipse@2x.jpg 2x" alt="A deep red eclipsed moon over a dark ridge" width="900" height="504" decoding="async" loading="lazy">
            <div class="castpop">
              <div class="castpop-title">Playing on TV</div>
              <div class="castrow pick">
                <span class="txt">
                  <span class="cast-name">Living room</span>
                  <span class="cast-model">Plays as it is</span>
                </span>
                <svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-conf"/></svg>
              </div>
              <div class="castrow">
                <span class="txt">
                  <span class="cast-name">Kitchen</span>
                  <span class="cast-model">Prepared before it starts</span>
                </span>
                <svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-conf"/></svg>
              </div>
              <div class="castrow">
                <span class="txt">
                  <span class="cast-name">Bedroom</span>
                  <span class="cast-model">Checking how this device can be reached…</span>
                </span>
                <span class="spin"></span>
              </div>
            </div>
            <div class="osc">
              <div class="times mono"><span>28:04</span><span>52:19</span></div>
              <div class="seekwrap">
                <div class="seektrack">
                  <span class="buffered" style="width:100%;background:rgba(255,255,255,.18)"></span>
                  <span class="fill" style="width:53%"></span>
                  <span class="knob" style="left:53%"></span>
                </div>
              </div>
              <div class="controls">
                <div class="cluster left">
                  <span class="b"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-volume"/></svg></span>
                  <span class="volume"><i></i><b></b></span>
                </div>
                <div class="cluster center">
                  <span class="b"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-prev"/></svg></span>
                  <span class="b play"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-pause"/></svg></span>
                  <span class="b"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-next"/></svg></span>
                </div>
                <div class="cluster right">
                  <span class="b opt"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-audio"/></svg></span>
                  <span class="b opt"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-subs"/></svg></span>
                  <span class="b cast">
                    <svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-cast"/></svg>
                    <span class="lit"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-cast"/></svg></span>
                    <svg class="walk" viewBox="0 0 24 24" aria-hidden="true"><use href="#i-cursor"/></svg>
                    <!-- The pointer is a child of the button it is pointing at, so
                         "on the icon" is arithmetic rather than a tuned offset: the
                         cursor's own tip sits at (3.3, 1.4) of its 19px box. -->
                          </span>
                  <span class="b"><svg viewBox="0 0 24 24" aria-hidden="true"><use href="#p-full"/></svg></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
